from django.urls import path, include
from bookstores import views

urlpatterns = [
    # path('list/',views.BookstoreListView.as_view(), name='list'),
    # path('create/',views.BookstoreCreateView.as_view(), name='create'),
    # path('detail/<int:pk>',views.BookstoreDetailView.as_view(), name='detail'),
    # path('update/<int:pk>',views.BookstoreUpdateView.as_view(), name='update'),
    # path('delete/<int:pk>',views.BookstoreDeleteView.as_view(), name='delete'),
]